import env from "codeforlife/settings"

export * from "codeforlife/settings"

export const {
  // Links.
  LINK_CFL,
  LINK_GH_CONTRIBUTING,
  // GitHub.
  GH_AUTH_URI,
  GH_SCOPE,
  GH_CLIENT_ID,
  GH_REDIRECT_URI,
} = env.vite
